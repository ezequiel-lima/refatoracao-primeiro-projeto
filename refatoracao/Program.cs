using System;

namespace refatoracao
{
    class Program
    {
        static void Main(string[] args)
        {
            //// Exercicio de extrair variavel
            //void GerarListagem()
            //{
            //    bool ehMac = (plataforma.ToUpper().IndexOf("MAC") > -1);
            //    bool ehChrome = (browser.ToUpper().IndexOf("CHROME") > -1);
            //    bool foiRedimensionado = redimensionado > 0;

            //    if (ehMac && ehChrome && foiInicializado() && foiRedimensionado)
            //    {
            //        // faça alguma coisa
            //    }
            //}
        }
    }
}
